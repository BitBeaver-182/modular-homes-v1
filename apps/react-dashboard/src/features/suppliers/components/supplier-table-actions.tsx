{/* <div className="flex justify-end gap-1">
            <Button
              size="icon-sm"
              type="button"
              variant="ghost"
              onClick={(): void => {
                setEditingSupplier(row.original);
              }}
            >
              <Pencil className="size-4" />
            </Button>
            <Button
              size="icon-sm"
              type="button"
              variant="ghost"
              onClick={(): void => {
                setDeleteDialogNonce((n) => n + 1);
                setDeletingSupplier(row.original);
              }}
            >
              <Trash2 className="size-4" />
            </Button>
          </div> */}